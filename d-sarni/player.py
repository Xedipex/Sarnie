class Player:

	def __init__(self, name, description, hp, sp, ingredients, weapons, combos, current_room):
		
		self.name = name
		self.hp = hp
		self.sp = sp
		self.ingredients = ingredients
		self.weapons = weapons
		self.current_room = current_room